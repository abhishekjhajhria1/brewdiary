# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# exported Ninkasi training data (user chat — never commit)
/scripts/ninkasi/out/

# internal working folder — local-only planning/handoff, NOT shipped or shared.
/internal/

# Claude Code's LOCAL settings only. CLAUDE.md (the front door) and
# .claude/skills/ (the taste-engine design spec) ARE committed on purpose —
# a fresh clone needs both, or a new developer/AI starts blind.
/.claude/settings.local.json
/.claude/settings.json

# python caches (local tooling)
__pycache__/
*.pyc

# typescript
*.tsbuildinfo
