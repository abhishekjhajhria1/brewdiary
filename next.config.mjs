/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA-ready: Phase 2 will add a manifest + service worker. Structure kept clean for that.
};

export default nextConfig;
